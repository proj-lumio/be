# Lumio UI Components

Shared design system primitives (liquid containers, buttons, inputs, modals, palette) extracted from the Lumio frontend. Meant to be included as a git submodule in any project that needs the same visual language (frontend, back-office, etc.).
